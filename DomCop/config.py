import datetime

target_date = datetime.date.today() + datetime.timedelta(days=3)

traffic_pdd_days=3
traffic_pdd_days_used = datetime.date.today() + datetime.timedelta(days=traffic_pdd_days)

traffic_gd_days=5
traffic_gd_days_used = datetime.date.today() + datetime.timedelta(days=traffic_gd_days)

exp_date_today = datetime.date.today()